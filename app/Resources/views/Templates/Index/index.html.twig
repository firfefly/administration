{% extends 'Templates/base.html.twig' %}
{% block stylesheets %}
        <link rel="stylesheet" type="text/css" href="{{asset('css/main.css')}}" />
{% endblock %}

{% block menu %}

   {{ parent() }}

{% endblock %}

{% block body %}
    {% if brand is not null %}
        <h1 class="titre_form"> Vous travaillez sur {{brand.getName()}} </h1>
    {% else %}
        <h1 class="titre_form"> Choisissez une marque.</h1>
    {% endif %}
<hr align="left" class="séparateur">
<div class="row">
    <div class="col-sm-12">
        <form method="POST">
            <table>
                <tr>
                    <td> {{ form_label (form.brands) }} </td>
                    <td> {{ form_widget (form.brands) }} </td>
                </tr>
            </table>
            {{ form_widget(form.save) }}
            {{ form_rest(form) }}
            {{ form_end(form) }}
        </form>
    </div>
</div>

{% endblock %}
