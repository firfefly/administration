{% extends 'Templates/base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="{{asset('css/main.css')}}" />
{% endblock %}
{% block menu %}
    {{ parent() }}
    <h1 class="titre_form"> {% if app.session.get('choosenBrand') is not null %} Les avis {{app.session.get('choosenBrand').getName()}} {% endif %} </h1>
    <hr align="left" class="séparateur">
{% endblock %}
{% block body %}
    <div class="row">
        <div class="col-sm-12">
            <form method="POST" enctype="multipart/form-data">
                <table id="formTab">
                    <tr>
                        <td> {{ form_label (form.text) }} </td>
                        <td> {{ form_widget (form.text) }} </td>
                    </tr>
                    <tr>
                        <td> {{ form_label (form.who) }} </td>
                        <td> {{ form_widget (form.who) }} </td>
                    </tr>
                    <tr>
                        <td> {{ form_label (form.image1) }} </td>
                        <td> {{ form_widget (form.image1) }} </td>
                    </tr>
                    <tr>
                        <td> {{ form_label (form.image2) }} </td>
                        <td> {{ form_widget (form.image2) }} </td>
                    </tr>
                    <tr>
                        <td> {{ form_label (form.date) }} </td>
                        <td> {{ form_widget (form.date) }} </td>
                    </tr>
                    <tr>
                        <td> {{ form_label (form.mark) }} </td>
                        <td> {{ form_widget (form.mark) }} </td>
                    </tr>
                </table>
                <div id="formButton">
                    {{ form_widget(form.save) }}
                    <button type="button" id="backButton" onclick="document.location.replace('{{path('reviews')}}');"> Retour</button>
                </div>
                {{ form_rest(form) }}
                {{ form_end(form) }}
            </form>
        </div>
    </div>
{% endblock %}

