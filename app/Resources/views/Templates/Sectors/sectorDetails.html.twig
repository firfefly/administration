{% extends 'Templates/base.html.twig' %}
{% block stylesheets %}
        <link rel="stylesheet" type="text/css" href="{{asset('css/main.css')}}" />
{% endblock %}
{% block menu %}
    {{ parent() }}
    <h1 class="titre_form"> {% if app.session.get('choosenBrand') is not null %} Les secteurs {{app.session.get('choosenBrand').getName()}} {% endif %} </h1>
    <hr align="left" class="séparateur">

{% endblock %}

{% block body %}
<div class="row">
    <div class="col-sm-12">
        <form method="POST" enctype="multipart/form-data">
            <table id="formTab">
                <tr>
                    <td> {{ form_label (form.code) }} </td>
                    <td> {{ form_widget (form.code) }} </td>
                </tr>
                <tr>
                    <td> {{ form_label (form.agencyId) }} </td>
                    <td> {{ form_widget (form.agencyId) }} </td>
                </tr>
            </table>
            <div id="formButton">
                {{ form_widget(form.save) }}
                <button type="button" id="backButton" onclick="document.location.replace('{{path('sectors')}}');"> Retour</button>
            </div>
            {{ form_rest(form) }}
            {{ form_end(form) }}
        </form>


    </div>
</div>
{% endblock %}

